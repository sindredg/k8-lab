# Allows traffic and health checks from Google Cloud's fixed load balancer IP ranges.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: golden-hour-allow-gateway
  namespace: demo
  labels:
    app.kubernetes.io/name: golden-hour
    app.kubernetes.io/part-of: k8-lab
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: golden-hour

  policyTypes:
    - Ingress

  ingress:
    # Uses ipBlock to target external Google IPs instead of internal Pods.
    - from:
        - ipBlock:
            cidr: 130.211.0.0/22
        - ipBlock:
            cidr: 35.191.0.0/16
      ports:
        - protocol: TCP
          port: 8080
---
# Allows labelled clients in the namespace to reach the Golden Hour Pods, which is what the pipeline's smoke test is.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: golden-hour-allow-http
  namespace: demo
  labels:
    app.kubernetes.io/name: golden-hour
    app.kubernetes.io/part-of: k8-lab
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: golden-hour

  policyTypes:
    - Ingress

  ingress:
    - from:
        - podSelector:
            matchLabels:
              golden-hour-client: "true"
      ports:
        - protocol: TCP
          port: 8080
---
# Allows those same clients to open the connection, which their own default deny egress blocks.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: golden-hour-client-allow-egress
  namespace: demo
  labels:
    app.kubernetes.io/part-of: k8-lab
spec:
  podSelector:
    matchLabels:
      golden-hour-client: "true"

  policyTypes:
    - Egress

  egress:
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: golden-hour
      ports:
        - protocol: TCP
          port: 8080
