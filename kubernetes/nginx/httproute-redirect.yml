# Answers plain HTTP with a permanent redirect to HTTPS. The redirect is issued at the load balancer, so no request reaches a Pod in clear text.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx-https-redirect
  namespace: demo
  labels:
    app.kubernetes.io/part-of: k8-lab
spec:
  parentRefs:
    - name: external
      sectionName: http     # attaches to the HTTP listener only

  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301