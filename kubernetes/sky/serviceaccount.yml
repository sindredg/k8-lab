# Gives the Golden Hour workload its own identity instead of the namespace default.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-hour
  namespace: demo
  labels:
    app.kubernetes.io/name: golden-hour
    app.kubernetes.io/part-of: k8-lab
automountServiceAccountToken: false
