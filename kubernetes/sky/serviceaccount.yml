# Gives the sky workload its own identity instead of the namespace default.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sky
  namespace: demo
  labels:
    app.kubernetes.io/name: sky
    app.kubernetes.io/part-of: k8-lab
automountServiceAccountToken: false
