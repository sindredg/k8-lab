# Gives the NGINX workload its own identity instead of the namespace default.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nginx
  namespace: demo
  labels:
    app.kubernetes.io/name: nginx
    app.kubernetes.io/part-of: k8-lab
automountServiceAccountToken: false